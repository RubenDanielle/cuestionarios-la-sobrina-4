<!DOCTYPE html>
<html>
	<head>
		<title>Lista de carreras disponibles</title>
	</head>
	<body>
		<h1>Carreras disponibles</h1>
		<ul>
			<% @careers.map do |career| %>
		 		<li>
		 			<a href="careers/<%= career.id%>"> <%= career.name %></a>
		 			(aparece en : <%= career.surveys.count.to_s %> cuestionarios)
		 		</li>
		 	<%end%>
		</ul>
  		<hr> 
		<h3>Agregar carrera: </h3>
		<form action = '/careers' method='POST' class="form-careers">
			<div class="form-careers">
				<label for="name">Nombre: </label>
				<input type='text' name='name' id="name" required>
			</div>
			<div class="form-careers">
				<input type='submit' value='Enviar'>
			</div>
  		<form>
	</body>
</html>
