<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="styles/style_surveys_by_date.css">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
    	Resultado de la busqueda
    </title>
</head>
<body>
    <h1>Usuarios que obtuvieron la carrera <%= @career_name %> entre los días <%= @first_date.to_date() %> y <%= @last_date.to_date() %>:</h1>
    <% if @surveys != nil %>
    	<ul>
            <% @surveys.map do |survey| %>
                <p><%= survey.username %>: <%= survey.created_at.to_date() %></p>
            <%end%>
        </ul>
    <% else %>
        <h2> No se ha encontrado una respuesta de cuestionario que coincida con los parámetros dados. </h2>
    <% end %>
</body>
</html>